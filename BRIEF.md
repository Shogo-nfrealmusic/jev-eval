# BRIEF — Jev vs LLM ベンチマーク

**このファイルを読んでから作業を始めてください。**

---

## これは何か

**2026年9月15日に TypeSafe AI が公開した「Jev」という判断特化モデルを、実データに近いデータセットで検証する。**

Jev は文章を生成せず、**型付きの質問に対して確率付きの判断（choice / score / boolean）だけを返す**モデル。
TypeSafe は自社評価で「LLM比 193.6倍速く、444.6倍安い」と公表しているが、**第三者による検証はまだ存在しない**（公開から3日）。

**ここでやるのは、その第三者検証。**結果はブログ記事として公開する。

---

## 3つのゴール

```
1. Jev を実際に触って、どこで効いてどこで壊れるかを掴む
2. 実装・計測・判断を1本に通し、技術力の証明物にする
3. 結果をブログ記事にできる状態（数字と誤答例）まで持っていく
```

**「速かった」で終わらせない。Jev が負ける場面を必ず1つ以上見つけて記録する。**

---

## 題材

**訪日外国人向け写真撮影サービス（実在の事業）に届く、予約に関する問い合わせの振り分け。**

現在この振り分けは人間が手でやっている。**そこを Jev で置き換えられるかを測る。**

> ⚠️ **実際の顧客データは一切使わない。**個人情報が含まれるため。
> **実務に即した合成データを作る。**

---

## 環境

| | |
|---|---|
| Node | v26.5.0（確認済み） |
| npm | 11.17.0（確認済み） |
| 言語 | **TypeScript** |
| SDK | `ai`（AI SDK）**7.0.105 以降**。`experimental_evaluate` が必要 |
| 認証 | **Vercel AI Gateway のみ。**`AI_GATEWAY_API_KEY`（`vck_` で始まる） |

**Jev も比較用のLLMも、同じ Gateway 経由で呼ぶ。**キーは1本で足りる。

```bash
cd ~/jev-eval
npm init -y
npm install ai@latest
npm install -D typescript tsx @types/node
npx tsc --init
```

**`.env.local` にキーを置き、`.gitignore` に必ず追加する。**

```
.env.local
node_modules
results/raw/
```

> **キーを絶対にコミットしない。**コード内にハードコードしない。

---

## 作るもの

```
~/jev-eval/
  BRIEF.md              このファイル
  src/
    data.ts             データセット（60件＋正解ラベル）
    types.ts            質問の定義・共通型
    jev.ts              Jev を呼ぶ
    baseline.ts         比較用LLMを呼ぶ（構造化出力）
    run.ts              両方を走らせて計測する
    score.ts            正解率・レイテンシ・コストを集計
  results/
    raw/                生の応答（gitignore）
    summary.json        集計結果
  RESULTS.md            表と誤答例（記事の材料）
  README.md             再現手順
```

---

## データセット（`src/data.ts`）

**60件。**言語と種類を散らす。

```
言語     英語 30 / 中国語 12 / 韓国語 9 / 日本語 9
```

**カテゴリ（8種類・これが choice の選択肢）**

```
reschedule        日程変更
cancel            キャンセル
refund            返金
location          撮影場所についての質問
pricing           料金・プランの質問
group_size        人数の変更
late_arrival      当日の遅刻・遅延
weather           天候による相談
```

**各件に、手で正解ラベルを付ける。**

```ts
type Case = {
  id: string;
  lang: "en" | "zh" | "ko" | "ja";
  text: string;
  expected: {
    category: Category;       // 上の8つ
    urgency: "low" | "high";  // score を 0.5 で二値化して評価する
    needsHuman: boolean;      // 人間の確認が必要か
  };
};
```

**作り方の指示**

- **40件は素直なケース。**1つのカテゴリに明確に当てはまる
- **20件は意図的に難しくする。**これが記事の中身になる
  - 2つのカテゴリにまたがる（「台風で飛行機が飛ばないので、明日を土曜に変えたい」→ weather と reschedule）
  - 感情的で情報が少ない（「最悪です。どうなってるんですか」）
  - 敬語や方言、タイプミス、絵文字混じり
  - 短すぎる（「明日何時？」）
  - 撮影と無関係（「このメール配信を止めてください」）
- **難しいケースの `expected` は、迷った理由をコメントで残す。**記事に引用する

**ラベルを付け終えたら、いったん止めて Shogo に15件ほど見せて確認を取ること。**
**正解ラベルが間違っていると、以降の測定が全部無意味になる。**

---

## 聞く質問（`src/types.ts`）

**Jev と LLM に、まったく同じ3つを聞く。**

```
category      choice    8つの選択肢から1つ
urgency       score     0〜1（0.5以上を high とみなす）
needs_human   boolean   人間が確認すべきか
```

**プロンプト・指示文は両者で完全に同じ文面にする。**片方だけ工夫しない。

---

## 実装

### Jev（`src/jev.ts`）

```ts
import { experimental_evaluate as evaluate } from "ai";

const result = await evaluate({
  model: "typesafe-ai/jev",
  state: caseText,
  questions: { /* category / urgency / needs_human */ },
});
```

**返ってくる確率も全部保存する。**信頼度と正解率の関係を見るため。

### 比較用LLM（`src/baseline.ts`）

**モデル: `openai/gpt-4o-mini`（Gateway経由）。**呼べなければ `anthropic/claude-haiku-4-5` に切り替え、**使ったモデル名を RESULTS.md に明記する。**

**`generateObject` で構造化出力にする。**JSONを文字列でパースさせる形は使わない（それだと LLM 側に不利になりすぎる）。

### 走らせ方（`src/run.ts`）

**測定の公平性のため、以下を守る。**

```
1. 直列で実行する（並列にするとレイテンシが歪む）
2. 最初の1件はウォームアップとして捨てる
3. 全60件を3周し、レイテンシは中央値を取る
4. Jev と LLM で、入力の順序を同じにする
5. 各応答の raw を results/raw/ に保存する
```

**レイテンシは「API呼び出しの直前から応答受領まで」を計測する。**

### 集計（`src/score.ts`）

```
category      正解率（%）と、カテゴリ別の内訳
urgency       0.5で二値化した一致率
needs_human   一致率。加えて「見逃し」（人が必要なのに false）を別に数える
レイテンシ     p50 / p95
コスト         1,000件あたりの実費
```

**コストは推定ではなく、応答に含まれる usage から実測する。**
取れない場合は「Jev: 入力100万トークンあたり $0.042、出力は無料」という公表値を使い、**公表値であることを明記する。**

**さらに、Jev の確率を使った運用シミュレーションを1つ入れる。**

```
確率 > 0.95        自動処理
0.70 〜 0.95       追加確認
< 0.70             人間に回す
```

**この閾値だと、何%が自動処理に回り、そのうち何%が正しかったか。**
**これが記事で一番価値のある数字になる。**（速度や価格は誰でも測れるが、これは実務の話）

---

## 出力（`RESULTS.md`）

**以下を必ず含める。**

1. 使ったモデル名とバージョン、実行日時
2. 結果の表（正解率・レイテンシ p50/p95・1,000件あたりコスト）
3. **Jev が外した5件**（原文・正解・Jevの答え・確率）
4. **LLM が外した5件**
5. カテゴリ別の正解率
6. 運用シミュレーションの結果
7. **この検証の限界**（合成データ・1人がラベル付け・単一環境・n=60）

**Jev が勝った点と負けた点を、両方書く。**片方だけだと宣伝記事になる。

---

## 終わったら

```
git init
gh repo create jev-eval --public --source=. --push
```

**そのうえで、RESULTS.md の内容を Shogo に渡す。**
**ブログ記事の文章は、別セッションで Shogo と Claude が書く。ここでは書かない。**

---

## 禁止

- 実際の顧客データを使う
- APIキーをコミット、またはコードに直接書く
- Jev に有利・不利な条件差を作る（プロンプト、リトライ回数、並列度）
- 測っていない数字を書く
- 結果が出る前に結論を書く

---

## 詰まったとき

**Jev がまだ early access で呼べない場合、その時点で止めて Shogo に報告する。**
**代替案を勝手に始めない。**
